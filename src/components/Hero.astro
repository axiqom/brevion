---
import HeroContactForm from './HeroContactForm';
import { withBase } from '../lib/base';

const trustChips = [
  '5-axis milling',
  'Aerospace metals & plastics',
  'Proto → production',
];

/** Capability language — not a cert/clock/tolerance billboard. */
const stats = [
  { value: '5-axis', label: 'CNC milling' },
  { value: 'Metals + plastics', label: 'shop mix' },
  { value: 'Proto → prod', label: 'one shop' },
];
---
<section class="relative flex min-h-[100svh] items-stretch overflow-x-hidden overflow-y-visible bg-zinc-950">
  <div class="absolute inset-0 overflow-hidden bg-zinc-950">
    <picture>
      <source media="(min-width: 768px)" srcset={withBase('hero-cnc.jpg')} />
      <img
        src={withBase('hero-cnc-sm.jpg')}
        alt=""
        class="h-full w-full object-cover object-center opacity-75"
        aria-hidden="true"
      />
    </picture>
    <div class="absolute inset-0 bg-gradient-to-t from-zinc-950 via-zinc-950/40 to-zinc-950/10"></div>
    <div class="absolute inset-0 bg-gradient-to-r from-zinc-950/90 via-zinc-950/40 to-transparent"></div>
    <div class="absolute inset-y-0 right-0 hidden w-[55%] bg-gradient-to-l from-zinc-950/90 via-zinc-950/40 to-transparent md:block" aria-hidden="true"></div>
  </div>

  <div class="relative z-10 mx-auto flex w-full max-w-7xl flex-col justify-center px-4 pb-14 pt-24 sm:px-6 sm:pb-16 md:pb-20 md:pt-28 lg:px-8">
    <div class="grid items-start gap-8 md:grid-cols-2 md:items-center md:gap-10 lg:gap-14">
      
      {/* 1. Header & Text */}
      <div class="animate-fade-up opacity-0 md:col-start-1 md:row-start-1 md:self-end md:pb-4" style="animation-delay: 0.05s">
        <div class="mb-4 flex items-center gap-2.5 sm:mb-5 sm:gap-3">
          <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-white sm:h-9 sm:w-9">
            <img
              src={withBase('milltrue-mark.svg')}
              alt=""
              width={20}
              height={20}
              class="h-4 w-4 sm:h-[18px] sm:w-[18px]"
              aria-hidden="true"
            />
          </div>
          <p class="font-display text-lg font-bold uppercase tracking-[0.16em] text-white/90 sm:text-xl sm:tracking-[0.2em] md:text-2xl md:tracking-[0.22em]">
            MillTrue
          </p>
        </div>

        <p class="mb-3 text-[11px] font-bold uppercase tracking-[0.2em] text-zinc-400 sm:mb-3.5 sm:text-xs sm:tracking-[0.22em]">
          Precision CNC · Aerospace / Defense / Robotics
        </p>

        <h1 class="mb-3 max-w-xl text-[1.85rem] font-bold uppercase leading-[0.98] tracking-tight text-white sm:mb-4 sm:text-4xl md:text-5xl lg:text-[3.25rem]">
          Precision parts, machined to your print.
        </h1>
        <p class="max-w-lg text-sm font-medium leading-relaxed text-zinc-300 sm:text-base md:text-lg">
          Tell us the part, material, and quantity. We DFM before the first chip — and keep documentation ready when your program needs it.
        </p>
      </div>

      {/* 2. Form (Primary CTA) */}
      <div
        id="intake"
        class="animate-fade-up w-full max-w-md scroll-mt-28 opacity-0 md:col-start-2 md:row-span-2 md:ml-auto md:max-w-none"
        style="animation-delay: 0.12s"
      >
        <div id="contact">
          <HeroContactForm client:load />
        </div>
      </div>

      {/* 3. Capabilities & Trust (secondary — not a stats billboard) */}
      <div class="animate-fade-up opacity-0 md:col-start-1 md:row-start-2 md:self-start md:pt-2" style="animation-delay: 0.15s">
        <dl class="mb-5 flex flex-wrap items-center gap-x-5 gap-y-3 sm:mb-6 sm:gap-x-7" aria-label="Key capabilities">
          {stats.map((stat, i) => (
            <>
              {i > 0 && <div class="hidden h-8 w-px bg-zinc-700/40 sm:block" aria-hidden="true"></div>}
              <div class="flex flex-col">
                <dt class="font-display text-base font-bold tracking-tight text-zinc-100 sm:text-lg md:text-xl">
                  {stat.value}
                </dt>
                <dd class="mt-0.5 text-[10px] font-bold uppercase tracking-wider text-zinc-500 sm:text-[11px]">
                  {stat.label}
                </dd>
              </div>
            </>
          ))}
        </dl>

        <ul class="mb-6 flex flex-wrap items-center gap-2.5 text-[10px] font-bold uppercase tracking-wider text-zinc-400 sm:mb-8 sm:text-[11px]" aria-label="Trust signals">
          {trustChips.map((chip, i) => (
            <>
              <li>{chip}</li>
              {i < trustChips.length - 1 && <li aria-hidden="true" class="text-zinc-600">&middot;</li>}
            </>
          ))}
        </ul>

        <a
          href={withBase('rfq')}
          class="inline-flex min-h-11 items-center text-sm font-medium text-zinc-400 underline-offset-4 transition-colors hover:text-zinc-200 hover:underline"
        >
          Prefer drawings or CAD? Full RFQ →
        </a>
      </div>

    </div>
  </div>
</section>
