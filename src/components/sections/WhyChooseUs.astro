---
const points = [
  'Experienced Engineers',
  'Modern Equipment',
  'Tight Tolerances',
  'Reliable Lead Times',
  'Quality Inspection',
];
---
<section id="why-brevion" class="scroll-mt-28 border-t border-aluminum/35 bg-aluminum/10 py-28 md:py-40">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid gap-16 lg:grid-cols-12 lg:gap-28">
      <div class="lg:col-span-4">
        <p class="mb-5 text-[11px] font-semibold uppercase tracking-[0.28em] text-aluminum">
          Why Brevion
        </p>
        <h2 class="text-3xl font-bold uppercase tracking-tight text-carbon md:text-4xl lg:text-5xl">
          Built to tolerance
        </h2>
      </div>
      <ul class="grid grid-cols-1 gap-x-14 gap-y-0 sm:grid-cols-2 lg:col-span-8">
        {points.map((point) => (
          <li class="border-t border-aluminum/40 py-8 text-lg font-medium text-carbon md:text-xl lg:text-2xl">
            {point}
          </li>
        ))}
      </ul>
    </div>
  </div>
</section>
