---
import { PenTool, Target, Layers } from 'lucide-react';

const pillars = [
  {
    icon: PenTool,
    title: 'DFM before chips fly',
    desc: 'Tolerance stack-ups, cost drivers, and material callouts reviewed before the first toolpath.',
    featured: false,
  },
  {
    icon: Target,
    title: 'First article to production',
    desc: 'Repeatable setups and fixturing so article one matches part one thousand.',
    featured: true,
  },
  {
    icon: Layers,
    title: 'One owner, clear lead time',
    desc: 'A single point of contact from quote through finishing, inspection, and ship.',
    featured: false,
  },
];
---
<section class="relative overflow-hidden border-b border-aluminum/40 bg-porcelain py-20 md:py-24">
  <div class="relative z-10 mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="mx-auto mb-12 max-w-2xl text-center md:mb-16">
      <p class="mb-3 text-[11px] font-bold uppercase tracking-[0.22em] text-aluminum">
        How we work
      </p>
      <h2 class="mb-4 text-3xl font-bold uppercase tracking-tight text-carbon md:text-4xl lg:text-5xl">
        Predictable manufacturing.
      </h2>
      <p class="text-base font-medium text-carbon/65 md:text-lg">
        Specs in. Lead time out. No black-box shop floors.
      </p>
    </div>

    <div class="grid grid-cols-1 gap-8 md:grid-cols-3 md:gap-10">
      {pillars.map((pillar) => (
        <div class={`rounded-xl border border-aluminum/35 bg-porcelain p-6 shadow-[0_12px_32px_rgba(69,63,58,0.06)] md:rounded-2xl md:p-8 ${pillar.featured ? 'md:border-aluminum/50' : ''}`}>
          <div class="mb-5 flex h-11 w-11 items-center justify-center rounded-lg bg-carbon text-porcelain">
            <pillar.icon size={20} strokeWidth={1.5} aria-hidden="true" />
          </div>
          <h3 class="mb-2.5 text-xl font-bold uppercase tracking-tight text-carbon md:text-2xl">
            {pillar.title}
          </h3>
          <p class="text-sm leading-relaxed text-carbon/65 md:text-base">
            {pillar.desc}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>
