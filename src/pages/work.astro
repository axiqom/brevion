---
import BaseLayout from '../layouts/BaseLayout.astro';
import { withBase } from '../lib/base';
import MobileConvertBar from '../components/MobileConvertBar';

const projects = [
  {
    title: 'Titanium airfoil',
    material: 'Titanium',
    focus: 'Contour',
    img: withBase('media/port-01.jpg'),
    alt: 'Transonic turbine blade cascade in a precision test rig',
  },
  {
    title: 'Aluminum housing',
    material: 'Aluminum',
    focus: 'Bore finish',
    img: withBase('media/port-02.jpg'),
    alt: 'Machined aluminum alloy housings with finished flange faces and threaded bores',
  },
];
---
<BaseLayout
  title="Work — Brevion"
  description="Sample CNC machining work. Material and focus for representative jobs."
  currentPage="work"
>
  <section class="border-b border-aluminum/35 bg-porcelain pt-24 pb-14 sm:pt-28 sm:pb-20 md:pt-36 md:pb-28">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <p class="mb-5 text-[11px] font-semibold uppercase tracking-[0.28em] text-aluminum sm:mb-6">
        Work
      </p>
      <h1 class="max-w-3xl text-3xl font-bold uppercase leading-[0.96] tracking-tight text-carbon sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl">
        Sample work
      </h1>
      <p class="mt-6 max-w-lg text-base font-medium leading-relaxed text-carbon/60 sm:mt-8 md:text-lg">
        Representative jobs — material and focus.
      </p>
    </div>
  </section>

  <section class="bg-porcelain py-14 sm:py-20 md:py-28">
    <div class="mx-auto max-w-7xl space-y-16 px-4 sm:space-y-24 sm:px-6 md:space-y-32 lg:px-8">
      {projects.map((proj, idx) => (
        <div
          class={`flex flex-col items-stretch gap-8 sm:gap-12 lg:gap-20 ${
            idx % 2 !== 0 ? 'lg:flex-row-reverse' : 'lg:flex-row'
          }`}
        >
          <figure class="group relative aspect-[4/3] w-full overflow-hidden rounded-xl bg-carbon sm:aspect-[5/4] md:rounded-2xl lg:w-[58%]">
            <img
              src={proj.img}
              alt={proj.alt}
              class="absolute inset-0 h-full w-full object-cover opacity-90 transition-transform duration-700 motion-reduce:transition-none group-hover:scale-[1.03]"
              loading="lazy"
              decoding="async"
            />
            <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-carbon/40 to-transparent" aria-hidden="true" />
          </figure>

          <div class="flex w-full flex-col justify-center lg:w-[42%]">
            <h2 class="mb-8 text-xl font-bold uppercase leading-tight tracking-tight text-carbon sm:mb-12 sm:text-2xl md:mb-14 md:text-3xl lg:text-4xl">
              {proj.title}
            </h2>
            <div class="flex gap-10 border-t border-aluminum/40 pt-6 sm:gap-14 sm:pt-8">
              <div>
                <span class="mb-2 block text-[11px] font-semibold uppercase tracking-widest text-aluminum">
                  Material
                </span>
                <span class="text-base font-semibold text-carbon sm:text-lg md:text-xl">{proj.material}</span>
              </div>
              <div>
                <span class="mb-2 block text-[11px] font-semibold uppercase tracking-widest text-aluminum">
                  Focus
                </span>
                <span class="text-base font-semibold text-carbon sm:text-lg md:text-xl">{proj.focus}</span>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <section id="cta" class="relative overflow-hidden border-t border-aluminum/40 bg-carbon py-20 sm:py-28 md:py-36">
    <div class="absolute inset-0">
      <img
        src={withBase('media/bg-cta.jpg')}
        alt=""
        class="h-full w-full object-cover opacity-30"
        aria-hidden="true"
        loading="lazy"
        decoding="async"
      />
      <div class="absolute inset-0 bg-gradient-to-r from-carbon via-carbon/85 to-carbon/60" aria-hidden="true" />
    </div>
    <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="flex flex-col items-stretch justify-between gap-8 sm:items-start sm:gap-10 md:flex-row md:items-center">
        <h2 class="max-w-xl text-2xl font-bold uppercase tracking-tight text-porcelain sm:text-3xl md:text-4xl lg:text-5xl">
          Ready to Start?
        </h2>
        <a
          href={withBase('rfq')}
          class="inline-flex min-h-12 w-full items-center justify-center rounded-lg bg-porcelain px-8 py-4 text-sm font-semibold uppercase tracking-widest text-carbon transition-colors duration-150 hover:bg-gold sm:min-h-11 sm:w-auto"
        >
          Request a Quote
        </a>
      </div>
    </div>
  </section>
  <MobileConvertBar client:load />
</BaseLayout>
